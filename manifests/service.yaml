
      kind: Service
apiVersion: v1
metadata:
  name: test
  namespace: app
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  selector:
    app: test
  ports:
    - port: 5678 
      targetPort: 8080
